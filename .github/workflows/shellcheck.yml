name: Shell Script Checks

on:
  pull_request:
    branches: [main]
    paths:
      - 'backup'
      - '.github/workflows/shellcheck.yml'

jobs:
  lint:
    name: Lint shell scripts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run ShellCheck
        run: shellcheck backup

      - name: Run shfmt
        run: docker run --rm -v "$PWD:/mnt" mvdan/shfmt:v3 -i 2 -sr -ci -d /mnt/backup
